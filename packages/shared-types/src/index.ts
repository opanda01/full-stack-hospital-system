# OpenAPI'den üretilen tipler: `pnpm --filter @hastane/shared-types generate`
# Backend ayaktayken çalıştırın. Web/mobil bu paketi workspace dependency olarak kullanır.
export type paths = Record<string, unknown>;
export type components = Record<string, unknown>;
